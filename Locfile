platform :android

output_path 'RecargaTussam/src/main/res/'

source :google_drive, 
       :spreadsheet => 'RecargaTussam', 
       :login => ENV['GOOGLE_LOGIN'], 
       :password => ENV['GOOGLE_PASSWORD']

# If the app has [i] tags or something like that, uncomment this
# except :keys => '[\[][i][\]]'
